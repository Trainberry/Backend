# Server

This is the main API server for the Train project. It receives all registrations from trains, and acts as an interface between end users and trains.

No configuration required! Just build it (`go build -o build ./cmd`) and run it!

## API

API is documented in `/api` folder.